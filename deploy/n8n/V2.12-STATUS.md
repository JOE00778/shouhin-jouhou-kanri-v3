# Smikie N8N v2.12 · 状态报告（2026-05-17）

> Mac 端用 N8N REST API 远程修了 6 个 bug + 拿到飞书 secret + 火山方舟 key 后链路打通。
> 当前 N8N（n8n.smikie-cms.cc）实时跑的是「hardcode 临时版」，git/zip 是「$env 干净版」。
> Boss 在 Inspiron 端做完下面 3 件后即可丢掉 hardcode、`.env` 自动驱动。

---

## ✅ 已搞定（exec 30~40 连续 11 次 19/19 全 success）

| 维度 | 状态 | 证据 |
|---|---|---|
| N8N 19 节点链路 | ✅ 全跑通 | exec 30-40 status=success |
| 飞书绿卡片 | ✅ Boss 已收到 | exec 38 lark 返回 `{"code":0,"msg":"success"}` |
| 火山方舟 DeepSeek-V3.2 | ✅ 真出内容 | exec 39 usage=2263 tokens，7 国翻译齐 |
| Shopee category_recommend SG/BR | ✅ access_token 拿到 | n02b refresh_status=partial（2/7） |

**修复点**（commit `baffffb` 已落 main）：
- B3 火山方舟节点：`HTTP Request → jsCode`（绕开 N8N jsonBody 表达式解析坑）
- B2 SPU 聚合：`$input.all().map(i=>i.json)` 替代 Array.isArray fallback
- B1 URL：`$('变量提取').first().json.sku_master_url`（fix 跨节点引用）
- 变量提取 fallback：`market_list` ID → BR
- 飞书签名 / URL：硬编码 → `$env.LARK_BOT_SIGN_SECRET` + `$env.LARK_WEBHOOK_URL`
- n02b / B4 错误格式：`String(e)` → `fmt(e)`（JSON 字符串化 axios error object）
- cms-api `_placeholders` bug：`",".join("%s" * n)` → `",".join(["%s"] * n)`
- cms-api `item_shopify_tags` 表：`CREATE TABLE IF NOT EXISTS` 兜底

---

## ⏳ Boss 端待办（3 件，做完即不再依赖 Mac hardcode）

### 1. `.env` 同步 secret/key（5 分钟）

打开 `D:\Smikie-N8N\Smikie-N8N-Installer-v1.5\.env`，更新两行：

```bash
# 旧值 PEgR5l90HbYkMJdOqhTGQe 在飞书后台已失效，换成 Boss 今晚提供的：
LARK_BOT_SIGN_SECRET=N7RX0eSQxR6P6Lz3ovWmJ

# 之前为空，配火山方舟 API Key：
VOLC_ARK_API_KEY=ark-54a02e2c-e3ae-49c2-b049-d4f851d3ce94-2d49b
```

然后 force-recreate：
```powershell
cd D:\Smikie-N8N\Smikie-N8N-Installer-v1.5
docker compose up -d --force-recreate n8n
# cms-api 也读 VOLC_ARK_API_KEY 吗？不读。不用 recreate cms-api。
```

完成后用 Mac 这边触发 webhook 验证 `$env` 生效（我会做）。

### 2. 5 国 Shopee OAuth 重做（10 分钟）

n02b 诊断显示 cms-api 库里 PH/TW/MY/TH/VN 这 5 国 refresh_token 只有 **32 字符**（疑似 OAuth code 没换成 token，正常 refresh_token 长度 104 字符）。SG/BR 是 104 字符正常。

```powershell
cd D:\Smikie-N8N\Smikie-N8N-Installer-v1.5
foreach ($mk in @("TW","PH","MY","TH","VN")) {
    Write-Host ""
    Write-Host "========== OAuth: $mk ==========" -ForegroundColor Cyan
    .\installer\oauth-7-markets.ps1 -Market $mk
    Pause   # Boss 浏览器点 Confirm Authorization 后回车
}
```

每国流程：
- 浏览器自动开 Shopee Sandbox 授权页
- 选 `$mk` 国测试店铺 → Confirm Authorization
- Shopee 回跳 `smikie-cms.cc/api/automation/shopee/oauth-callback?code=xxx&...`
- cms-api 用 code 调 Shopee `/api/v2/auth/token/get` 拿 104 字符 refresh_token，持久化到 cms_postgres

5 国都跑完后跑 1 次 webhook 验证：
```powershell
.\installer\trigger-shopee-workflow.ps1 -Jans 4901872391141 -Markets TW,PH,MY,SG,TH,VN,BR -Local
```

期望 n02b `refresh_status=all_ok` + 7 国 access_token 全拿到 + B3.5 类目自动判定 7 国都出 `category_id`。

### 3. 完成后通知我

我立刻：
- 把当前 N8N hardcode workflow 推回 `$env` 版（PUT clean 版本进 N8N）
- 触发 1 次确认 `$env` 链路通
- 重 build `v2.12.zip`（含本次所有修复 + clean workflow）

---

## 🔮 v2.12 之后路线

完成上面 3 件 → v2.12 是 T-314 真正完工版。后续：
- **T-315**：补 `cat-shopee-map.csv`（手工映射表），让 B3.5 不依赖 Shopee category_recommend API
- **v2.13**：B5c 真上架（替换 stub）—— 要 Live Partner Key + Boss 拍板
- **v2.14**：7 国并行执行（当前 SPU loop 串行）

---

## 当前 N8N 跑的 workflow 状态（重要）

| 节点 | git/zip 版 | N8N 实时版 |
|---|---|---|
| 飞书签名 | `$env.LARK_BOT_SIGN_SECRET` | hardcode `N7RX0eSQxR6P6Lz3ovWmJ` |
| B3 LLM apiKey | `$env.VOLC_ARK_API_KEY` | hardcode `ark-54a02e2c-...` |
| 其他 19 节点 | 同 | 同 |

**意义**：Boss 不动 `.env` 现在飞书 + LLM 也正常出内容。Boss 改完 `.env` + force-recreate 后，我 PUT clean 版进 N8N，hardcode 彻底退出。
